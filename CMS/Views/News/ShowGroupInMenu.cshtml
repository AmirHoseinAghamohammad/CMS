@model IEnumerable<DataLayer.PageGroup>

<span>گروه های خبری</span>
<ul>
    @foreach (var item in Model)
    {
        <li><a href="/Group/@item.GroupID/@item.GruopTitle"> @item.GruopTitle</a></li>
    }

</ul>
